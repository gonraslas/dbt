name: sbx_test 
version: '1.0.0'
config-version: 2

# Carpeta de modelos
model-paths: ["models"]
analysis-paths: ["analysis"]
macro-paths: ["macros"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]

# Configuración de modelos
models:
  sbx_medallion:
    bronze:
      +materialized: view          
      salesforce:
        +database: PRO-BRZ  
        +schema: SALESFORCE         
    silver:
      +materialized: table          
      salesforce:
        +database: PRO-SLV
        +schema: RELATIONAL
    gold:
      +materialized: table      
      core:
        +database: PRO-GLD
        +schema: DIMENSIONAL   
